/** 汎用 UI 関連の定数 */

/** 共通 Select のドロップダウン最大高さ（px。max-h-60 と一致） */
export const DROPDOWN_MAX_HEIGHT = 240;

/** ドロップダウンとトリガー間の余白（px） */
export const DROPDOWN_GAP = 4;

/** テーブルのデフォルト1ページ件数 */
export const DEFAULT_PAGE_SIZE = 10;

/** チャットウィジェットを閉じるアニメーション時間（ms。globals.css の chat-slide-down と一致させる） */
export const CLOSE_ANIMATION_MS = 220;

/**
 * 売上バーのホバー時ツールチップの固定幅（px）。
 * 右端はみ出し判定と左反転量の計算に使うため、表示にも同じ値を適用して
 * 計測タイミングに依存させない。
 */
export const TOOLTIP_WIDTH = 160;

/** 売上パフォーマンスのラベル列幅（px） */
export const LABEL_WIDTH = 120;
